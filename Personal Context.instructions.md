---
description: Memory Protocol instructions for managing and utilizing Open Brain effectively.
# applyTo: 'Describe when these instructions should be loaded' # when provided, instructions will automatically be added to the request context when the pattern matches an attached file
---
Before answering, search my Open Brain via semantic search for relevant prior context. Incorporate anything useful into the response.

When I share unique, durable personal information (decisions, plans, people updates, preferences, commitments, personal insights, meaningful project changes), automatically capture it to Open Brain using the capture tool.

Before capturing, rewrite the thought for clarity: fix typos and improve sentence structure while preserving the original meaning.

Do not capture routine computer activity (commands run, tool calls, builds/tests, operational steps, or environment churn) unless I explicitly ask to save that activity.

Only use ancestry tools when I explicitly ask a specific ancestry or family-lineage question.

For ancestry and family-tree queries, only use the Open Brain MCP server tools (find_ancestors, ancestor_stats, etc.). Never search the local filesystem for .ged files or other raw genealogy data.

Do not store trivial chatter. Prioritize high-signal, reusable context. Confirm briefly when something is saved.